<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title class="titulo">Editar Película</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <figure class="logo">
        <a href="/"><img src="/img/logo-DH.png" alt="Logo Digital House"></a>
      </figure>
    <h1 class="titulo">Película a Editar:  <%= movie.title %></h1>
    <section class="formulario">
        <form method="POST" action="/movies/update/<%= movie.id %>?_method=PUT">
            <div>
                <label for="title">Titulo </label>
                <p><input type="text" name="title" id="title" value="<%= movie.title %>" required></p>
            </div>
            <div>
                <label for="rating">Rating </label>
                <p><input type="number" name="rating" id="rating" value="<%= movie.rating %>" required></p>
            </div>
            <div>
                <label for="awards">Premios </label>
                <p><input type="number" name="awards" id="awards" value="<%=movie.awards %>" required></p>
            </div>
            <div>
                <label for="release_date">Fecha De Estreno </label>
                <p><input type="date" name="release_date" id="release_date" value="<%= movie.release_date %>" required></p>
            </div>
            <div>
                <label for="length">Duracion </label>
                <p><input type="number" name="length" id="" value="<%= movie.length %>" required></p>
            </div>
            <div>
                <label for="genre">Genero </label>
                <p>
                    <select name="genre_id" id="genre">
                            <option value=""></option>
                        <% genres.forEach(genre => { %>
                            <option value="<%= genre.id %>" <%= genre.id == movie.genre_id ? "selected" : null %> ><%= genre.name %></option>
                        <% }) %> 
                    </select>
                </p>                
            </div>
            <div>
                <button class="botonAgregar" type="submit">Actualizar</button>
            </div>
            <div class="botonVolver">
                <a href="/actors"><button>Volver al Listado de Películas</button></a>                
            </div>
        </form>        
    </section>
</body>

</html>